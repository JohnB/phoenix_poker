<h1>New Attendee result</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.attendee_result_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.attendee_result_path(@conn, :index) %></span>
