<h1>Edit Game night</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.game_night_path(@conn, :update, @game_night)) %>

<span><%= link "Back", to: Routes.game_night_path(@conn, :index) %></span>
