<h2>New attendee result</h2>

<%= render "form.html", changeset: @changeset,
                        action: attendee_result_path(@conn, :create) %>

<%= link "Back", to: attendee_result_path(@conn, :index) %>
